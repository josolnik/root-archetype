# /simplify -- Code Simplification Review

Read and follow the instructions in `agents/skills/simplify/SKILL.md`.

Pass `$ARGUMENTS` as the file list if provided. If no arguments, review
recently modified files from `git diff` and `git diff --cached`.
